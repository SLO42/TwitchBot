{
  "name": "twitchbot_for_party_dad",
  "description": "express with Babel",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf ./dist/*",
    "build": "npm run clean && babel 'src' --out-dir 'dist' --source-maps",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": "6.11.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.6.4",
    "@babel/core": "^7.6.4",
    "@babel/preset-env": "^7.6.3"
  },
  "private": true,
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  }
}
